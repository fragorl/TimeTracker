package com.fragorl.timetracker.jobs;

import com.fragorl.timetracker.time.TimeSegment;
import com.google.common.collect.Range;
import org.junit.Test;
import static junit.framework.Assert.assertEquals;

/**
 * @author Alex
 * @version $Id$
 *          <p/>
 *          Created on 23/05/13 12:25 PM
 */
public class JobTest {

    public @Test void testTimeWorkedMillis_SingleSegment_HasCorrectJobLength() {
//        Job job = new Job("EntifyStrings", "EntifyStrings");
//        long jobLengthMillis = 10_000;
//        job.addTimeSegment(new TimeSegment(System.currentTimeMillis(), System.currentTimeMillis() + jobLengthMillis));
//        assertEquals(jobLengthMillis, job.timeWorkedMillis());
    }

}
