package com.fragorl.timetracker.jobs;

import com.google.common.collect.Range;
import org.junit.Test;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.Collections;
import java.util.List;

import static junit.framework.Assert.assertEquals;

/**
 * @author Alex
 * @version $Id$
 *          <p/>
 *          Created on 23/05/13 12:43 PM
 */
public class JobsListTest {

    public @Test void testJobComparison_Works() {
//
//        List<Job> jobs = new ArrayList<>();
//        Job first = new Job("first");
//        first.addTimeSegment(
//                Range.closedOpen(
//                        System.currentTimeMillis(),
//                        System.currentTimeMillis() + 10_000
//                )
//        );
//
//        Job second = new Job("second");
//        second.addTimeSegment(
//                Range.closedOpen(
//                        System.currentTimeMillis(),
//                        System.currentTimeMillis() + 5_000
//                )
//        );
//
//        jobs.add(first);
//        jobs.add(second);
//
//        System.out.println(jobs);
//
//        Collections.sort(jobs, (p1, p2) -> new Long(p1.timeWorkedMillis()).compareTo(p2.timeWorkedMillis()));
//
//        System.out.println(jobs);
//
//        assertEquals(Arrays.asList(second, first), jobs);
    }
}
